#!/bin/sh
IFS=:
set -- "${CODEBUILD_BUILD_ID}"
PROJECT=$1
VERSION_DATE=`date +"%Y%m%d_Build%H%M"`
PREFIX=""
case $PROJECT in
	FISN_Staging)
		PREFIX="rc"
		;;

	FISN_Production)
		PREFIX="v"
		;;
esac

export USE_VERSION="$PREFIX$VERSION_DATE"
